image: node:4

before_script:
 - npm set registry $NPM_REGISTRY

build:
  script:
   - npm install
   - npm run build
  artifacts:
    expire_in: 31d
    paths:
     - dist/

test:
  script:
   - npm install --production
   - npm test
